{
  "name": "frontend",
  "version": "1.0.0",
  "description": "This is the frontend part of the app. Install dependencies with `npm i` after cloning, then use `grunt` to build the app. This will produce some stuff in the `public` folder which should be served by the server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tristan Seifert",
  "license": "ISC",
  "dependencies": {
    "foundation-sites": "^6.5.1",
    "grunt": "^1.0.4",
    "grunt-contrib-copy": "^1.0.0",
    "grunt-contrib-htmlmin": "^3.0.0",
    "grunt-contrib-uglify": "^4.0.0",
    "grunt-sass": "^3.0.2",
    "jquery": "^3.3.1",
    "jquery.idle": "^1.3.0",
    "load-grunt-tasks": "^4.0.0",
    "node-sass": "^4.11.0"
  }
}
